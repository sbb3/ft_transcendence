// routes
// /users => get all users => array //withouth blocked users
// /users/:id => getUserById 

// /users/me => current palyer  

// users?username=username -> getUserByUsername
// users?email=  => getUserByEmail

// users/${id}/settings => updateUserSettings => patch  username and avatar , auth
// users/${id}/friends => getFriends
// users/${currentUserId}/friends => addFriend
// users/:id/friends/:friendId => deleteFriend

// leaderboard get leadeboard
// users/:id/recentgames/ => get recent games
// notifications => getNotifications
// notifications => sendNotification => post 
// /notifications/:id deleteNotification
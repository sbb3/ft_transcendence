import { Box, Flex } from '@chakra-ui/react';
import React from 'react';

const Leaderboard = () => {
    return (
        <Box flex={1} h='full' bg="green">
            Leaderboard
        </Box>
    );
};

export default Leaderboard;